import { IngredientsMeasuresPipe } from './ingredients-measures.pipe';

describe('IngredientsMeasuresPipe', () => {
  it('create an instance', () => {
    const pipe = new IngredientsMeasuresPipe();
    expect(pipe).toBeTruthy();
  });
});
